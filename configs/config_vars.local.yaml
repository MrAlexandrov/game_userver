worker-threads: 4
worker-fs-threads: 2
logger-level: debug

is-testing: false

server-port: 8080

grpc-server-port: 8081

# Local run: postgres via docker compose (ports exposed to localhost)
pg-connection: postgresql://user:password@localhost:5432/game_userver_db_1
